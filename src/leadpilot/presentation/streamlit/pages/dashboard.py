from __future__ import annotations

import streamlit as st


def render(container: object) -> None:
    st.title("Dashboard")
    st.info("Lead intelligence insights will appear here in a future milestone.")
